package com.example.miaula.network

enum class ApiErrorType {
    UNKNOWN_ERROR,
    EXCEPTION_ERROR,
    PASSWORD_FORMAT_WRONG,
    EMAIL_FORMAT_WRONG,
    COUNTRY_MANDATORY,
    ADVISOR_FORMAT_WRONG,
    USER_ALREADY_EXISTING,
    USER_ALREADY_VERIFIED,
    USER_NOT_VERIFIED,
    INCORRECT_USER_PASSWORD,
    LOGIN_ATTEMPTS_EXCEEDED,
    ACCOUNT_DUPLICATE,
    NOT_OWNER_ACCOUNT,
    ACCOUNT_ALREADY_EXISTING,
    ACCOUNT_INCORRECT,
    INVALID_PIN,
    INVALID_FILE_UPLOAD,
    INSUFFICIENT_FUNDS,
    BrokerError,
    GenericError,
    EXCHANGE_MIN_NOTIONAL,
    EXCHANGE_NO_AVAILABLE_AMOUNT,
    EXCHANGE_INSUFFICIENT_FUNDS,
    EXCHANGE_EXCEPTION,
    EXCHANGE_INVALID_QUANTITY,
    BIND_ERROR_GENERIC,
    BIND_ERROR_VW010,
    BIND_ERROR_VW011,
    BIND_ERROR_VW007,
}

data class ApiError(var type: ApiErrorType?, val message: String)